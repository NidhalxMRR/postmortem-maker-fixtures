# postmortem-maker fixtures

Public incident-record fixtures used by the `postmortem-maker` runx skill harness.
Each file is a plain-text incident thread fetched at run time by `web.fetch`.
They are intentionally small, stable, and public so the harness is reproducible
by anyone.
